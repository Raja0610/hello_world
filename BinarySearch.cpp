#include<iostream>
using namespace std;
/*
int binarySearch(int *arr,int size1,int x)
{
    int i,j,position,mid;
    i=0;
    j=size1-1;
    while(i<=j)
    {
        mid=(i+j)/2;
        if(arr[mid]==x)
        {
            position=mid;
            return position;
        }
        else if(x<=arr[mid])
        {
            j=mid-1;
        }
        else if(x>arr[mid])
        {
            i=mid+1;
        }
    }
    return -1;
}

int main()
{
    int a[100],n,i,item,result;
    cin>>n;
    for(i=0;i<n;i++)
    {
        cin>>a[i];
    }
    cin>>item;
    result=binarySearch(a,n,item);
    cout<<result;
    return 0;
}
*/
