#include<iostream>
using namespace std;
/*
void mergeSorted(int *input1,int *input2,int m,int n,int *output)
{
    int i,j,k;
    i=0;
    j=0;
    k=0;
    while(i<m&&j<n)
    {
        if(input1[i]<input2[j])
        {
            output[k]=input1[i];
            i++;
            k++;
        }
        else{
            output[k]=input2[j];
            k++;
            j++;
        }
    }
    while(i<m)
    {
        output[k]=input1[i];
        k++;
        i++;
    }
    while(j<n)
    {
        output[k]=input2[j];
        k++;
        j++;
    }
}

int main()
{
    int arr1[20],arr2[20],size1,size2,arr3[40],i;
    cin>>size1>>size2;
    for(i=0;i<size1;i++)
    {
        cin>>arr1[i];
    }
    for(i=0;i<size2;i++)
    {
        cin>>arr2[i];
    }
    mergeSorted(arr1,arr2,size1,size2,arr3);
    for(i=0;i<(size1+size2);i++)
    {
        cout<<arr3[i]<<" ";
    }
    return 0;
}
*/
